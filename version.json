{
  "version": "5.6.22",
  "major": 5,
  "minor": 6,
  "patch": 22,
  "date": "2026-08-19",
  "notes": "Moved the Overview Dashboard's Needs Attention card to below the charts grid (Storage Savings + Storage Capacity by System), instead of between the KPI row and the charts. The compiled build (index.html) already had it in this position -- only the dev-served file (index_src.html) needed the fix, so this only affects local development, not the packaged app."
}
