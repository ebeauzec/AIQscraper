{
  "version": "5.6.69",
  "major": 5,
  "minor": 6,
  "patch": 69,
  "date": "2026-09-17",
  "notes": "v5.6.68 replaced the fake hypervisor/database/backup detection in the SAM tab's big 'Storage Protocol & Data Protection Profile' card, but missed the small top-row KPI tile (samHypervisorCard) feeding the same fake data: the fleet view called getSystemIntegrations() (always 'Not Reported'), and the single-system view read sys.hypervisors, a field the harvester never populates. Repurposed both branches to use the same real per-SVM LIF protocol data (getSystemProtocolProfile()): fleet view shows a distinct-protocol count plus SAN/NAS system coverage, single-system view lists the system's actual protocols, SAN/NAS split, and SVM count."
}
