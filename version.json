{
  "version": "5.6.38",
  "major": 5,
  "minor": 6,
  "patch": 38,
  "date": "2026-08-19",
  "notes": "Fixed: 'Import Findings Into Tracker' was still scoped to whatever customer was selected at import time even after 5.6.37 fixed the dropdown sync -- so switching to a different customer correctly filtered the view but showed empty, since nothing had been imported for them, which looked like the same bug one layer deeper. Import now always scans every loaded customer regardless of the sidebar filter; only the table view respects the customer dropdown. Button relabeled 'Import Findings Into Tracker (All Customers)'. Also added sortable tracker columns -- Status, Severity, SLA, Customer, System, Title, Owner, Due Date, and Last Seen headers are now clickable with a visible sort direction indicator. Verified live: imported with a customer filter active, confirmed items landed across all loaded customers, confirmed the table still correctly scoped to one customer, and confirmed all 8 sortable columns work correctly in both directions."
}
