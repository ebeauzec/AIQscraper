{
  "version": "5.6.57",
  "major": 5,
  "minor": 6,
  "patch": 57,
  "date": "2026-09-16",
  "notes": "Full deliverables cleanup: read every generated document end-to-end against real live data and fixed what was actually wrong. Real bugs: doubled 'Customer: Customer:' scope headers, a verbatim-duplicated Architecture Roadmap paragraph, near-duplicate corrective-action entries grouped by cluster instead of by fix, a CVE Priority Matrix that could contradict its own document's CVE count, raw HTML leaking into plain-text output, and a Sustainability Report defaulting to a fabricated '0/100' instead of falling back to a real score. Conciseness: the TAM Success Plan's CVE section (82% of the document, 504 individually-listed bulletin rows) is now a real summary plus top 5, cut from 137KB to 37KB with no information silently dropped -- the full list stays in the dedicated Security Posture Brief."
}
