{
  "version": "4.7.2",
  "major": 4,
  "minor": 7,
  "patch": 2,
  "date": "2026-08-10",
  "notes": "Incorporated a follow-up verification pass on the platform slot/MetroCluster data. FAS8300 and FAS8700 get their own confirmed 7-PCIe-slot-per-controller backplate layout (was previously guessed into the 5-slot A400 bucket). C400 confirmed to genuinely match A400's 5-slot layout. FAS9000 confirmed at 10 slots/controller matching A900, sourced from a NetApp datasheet PDF rather than a live key-specs page since that platform predates the current doc format -- labeled as a lower-confidence confirmation, not a guess. MetroCluster ISL max-distance figures refined: IP (700km) and the blanket FC figure (300km) are now marked confirmed by primary sources (NetApp's own ISL page + TR-4705); the FC-Brocade-vs-FC-Other 200km split could not be found in any current NetApp primary source and is now explicitly flagged unconfirmed rather than silently stated as fact. Added a real RCF (Reference Configuration File) compliance signal to harvested switches, sourced from Active IQ's own rcfVersion field (already harvested, previously only used indirectly to compute a firmware target) -- Switch Validation now shows an explicit RCF Compliant / RCF Mismatch badge distinct from firmware currency. Clarified the per-port telemetry empty-state message for E-Series/SANtricity systems: Active IQ's networkPorts field only exists on the ONTAPSystem GraphQL type, so E-Series systems genuinely never have per-port Ethernet data -- confirmed live and reworded from a generic 'not reported' to an explicit explanation of why."
}
