{
  "version": "5.6.74",
  "major": 5,
  "minor": 6,
  "patch": 74,
  "date": "2026-09-20",
  "notes": "Harvest no longer discards valid data when Active IQ returns partial GraphQL errors (a null/NaN field on one or two systems): it kept treating them as total failure and dropped the whole account to a thinner query tier, losing monthlyCapacity and utilization % for every system. Now keeps the returned systems (verified: TAM tier succeeds, 167 systems). Also removed the always-failing Cisco FlexPod CVD fetch."
}
