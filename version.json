{
  "version": "5.6.15",
  "major": 5,
  "minor": 6,
  "patch": 15,
  "date": "2026-08-19",
  "notes": "Fixed the Contracts & Lifecycle Events tile counts being computed from a different, disconnected data source than the table right below them -- the tiles only read contracts.endDate while the table correctly reads Active IQ's lifecycleEvents feed, so a scope with empty contracts.endDate but real CRITICAL 'Contract Expiration' events at 13 days out (visible in the table) showed 'Expiring < 90 Days: 0' on the tile above it. Tile counts now union both sources. Also added 'Generated By: AIQ Advisor vX.X.X' to the Section 12 TXT export header for diagnosability -- used it to confirm the 5.6.12 recommendations account-scoping fix IS working correctly against real live data (37 -> 23 recs, zero duplicates for the same stc customer scope that previously showed the bug); a report still showing 37-with-duplicates was generated by a browser tab that hadn't reloaded the app since before that fix shipped, not a regression."
}
